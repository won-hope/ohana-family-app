services:
  db:
    image: postgres:16
    container_name: ohana-postgres
    environment:
      POSTGRES_USER: ohana
      POSTGRES_PASSWORD: ohana
      POSTGRES_DB: ohana
    ports:
      - "5432:5432"
    volumes:
      - ohana_pgdata:/var/lib/postgresql/data

volumes:
  ohana_pgdata:
